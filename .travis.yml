language: ruby
rvm:
  - 2.4.1
  - 2.4.0
service:
  - mysql
cache: bundler
before_script:
  - bundle exec rake db:create
  - bundle exec rake db:migrate
script:
  - bundle exec rubocop
  - bundle exec rails spec
